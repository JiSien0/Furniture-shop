<Window x:Class="FurnitureShop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FurnitureShop"
        mc:Ignorable="d"
        Title="Меблевий магазин - Самообслуговування" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        Icon="bench.ico">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                   Text="Каталог меблів"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="#333333"
                   Margin="0,0,0,20"/>

        <!-- Каталог меблів -->
        <Border Grid.Row="1" Grid.Column="0"
                Background="White"
                CornerRadius="8"
                Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="#CCCCCC" Opacity="0.3" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Доступні меблі"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#2196F3"
                           Margin="0,0,0,15"/>

                <!-- Панель пошуку та фільтрів -->
                <Grid Grid.Row="1" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Поле пошуку -->
                    <Border Grid.Column="0"
                            BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            CornerRadius="4"
                            Background="White">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Іконка лупи -->
                            <TextBlock Grid.Column="0"
                                       Text="🔍"
                                       FontSize="16"
                                       Foreground="#999999"
                                       VerticalAlignment="Center"
                                       Margin="10,0,5,0"/>

                            <Grid Grid.Column="1">
                                <TextBox x:Name="SearchBox"
                                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                         VerticalAlignment="Center"
                                         Padding="5,8"
                                         BorderThickness="0"
                                         FontSize="13"
                                         Background="Transparent"/>

                                <!-- Placeholder -->
                                <TextBlock Text="Пошук за назвою або серійним номером..."
                                           Foreground="#999999"
                                           FontSize="13"
                                           VerticalAlignment="Center"
                                           Margin="5,0,0,0"
                                           IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>

                            <Button Grid.Column="2"
                                    Content="✕"
                                    Command="{Binding ClearSearchCommand}"
                                    Padding="8,4"
                                    Margin="0,0,5,0"
                                    FontSize="14"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="#666666"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SearchText}" Value="">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding SearchText, Converter={x:Null}}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                        <Style.Resources>
                                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"/>
                                        </Style.Resources>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>

                    <!-- Випадаючий список категорій -->
                    <Grid Grid.Column="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- ComboBox з іконкою та текстом -->
                        <Grid Grid.Column="0">
                            <ComboBox ItemsSource="{Binding Categories}"
                                      SelectedItem="{Binding SelectedCategory}"
                                      MinWidth="150"
                                      Padding="8"
                                      FontSize="13"
                                      VerticalAlignment="Center">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📂 " FontSize="14" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>

                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Padding" Value="8,5"/>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>

                            <!-- Placeholder коли нічого не вибрано -->
                            <StackPanel Orientation="Horizontal"
                                        IsHitTestVisible="False"
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Center">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedCategory}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <TextBlock Text="📂 " FontSize="14" Foreground="#999999" Margin="0,0,5,0"/>
                                <TextBlock Text="Категорії" FontSize="13" Foreground="#999999" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <Button Grid.Column="2"
                                Content="✕"
                                Command="{Binding ClearCategoryCommand}"
                                Padding="8,6"
                                FontSize="13"
                                Background="#F5F5F5"
                                BorderThickness="1"
                                BorderBrush="#E0E0E0"
                                Cursor="Hand"
                                VerticalAlignment="Center">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedCategory}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Grid>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding FurnitureList}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FAFAFA"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="15"
                                        Margin="0,0,0,10"
                                        MouseLeftButtonUp="FurnitureItem_Click"
                                        Cursor="Hand"
                                        Tag="{Binding}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Зображення товару -->
                                        <Border Grid.Column="0"
                                                BorderBrush="#E0E0E0"
                                                BorderThickness="1"
                                                CornerRadius="4"
                                                Width="90"
                                                Height="90">
                                            <Image Source="{Binding ImagePath}"
                                                   Stretch="Uniform"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Style.Triggers>
                                                            <Trigger Property="Source" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                        </Border>

                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="{Binding Name}"
                                                       FontSize="16"
                                                       FontWeight="SemiBold"
                                                       Foreground="#333333"/>
                                            <TextBlock FontSize="11"
                                                       Foreground="#2196F3"
                                                       Margin="0,2,0,0">
                                                <Run Text="🏷️ "/>
                                                <Run Text="{Binding SerialNumber}"/>
                                                <Run Text=" | "/>
                                                <Run Text="{Binding Category.Name}" FontStyle="Italic"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding Description}"
                                                       FontSize="12"
                                                       Foreground="#666666"
                                                       TextWrapping="Wrap"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxHeight="40"
                                                       Margin="0,5,0,0"/>
                                            <TextBlock FontSize="14"
                                                       Foreground="#4CAF50"
                                                       FontWeight="Bold"
                                                       Margin="0,8,0,0">
                                                <Run Text="Ціна: "/>
                                                <Run Text="{Binding Price, StringFormat='{}{0:N2} ₴'}"/>
                                            </TextBlock>
                                            <TextBlock FontSize="11"
                                                       Foreground="#999999"
                                                       Margin="0,5,0,0">
                                                <Run Text="📍 Склад: Ряд "/>
                                                <Run Text="{Binding StorageRow}"/>
                                                <Run Text=", Полиця "/>
                                                <Run Text="{Binding StorageShelf}"/>
                                            </TextBlock>
                                            <TextBlock Text="▶ Клікніть для детального перегляду"
                                                       FontSize="10"
                                                       Foreground="#2196F3"
                                                       FontStyle="Italic"
                                                       Margin="0,5,0,0"/>
                                        </StackPanel>

                                        <Button Grid.Column="3"
                                                Content="Додати в кошик"
                                                Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                VerticalAlignment="Center"
                                                Padding="20,10"
                                                Click="AddToCart_Click"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Кошик -->
        <Border Grid.Row="1" Grid.Column="2"
                Background="White"
                CornerRadius="8"
                Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="#CCCCCC" Opacity="0.3" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Кошик"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#2196F3"
                           Margin="0,0,0,15"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Cart}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FAFAFA"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="10"
                                        Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0"
                                                   Text="{Binding Furniture.Name}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   TextWrapping="Wrap"/>

                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,0">
                                            <Button Content="-"
                                                    Width="25"
                                                    Height="25"
                                                    FontSize="14"
                                                    Padding="0"
                                                    Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"/>
                                            <TextBlock Text="{Binding Quantity}"
                                                       FontSize="13"
                                                       VerticalAlignment="Center"
                                                       Margin="10,0"/>
                                            <Button Content="+"
                                                    Width="25"
                                                    Height="25"
                                                    FontSize="14"
                                                    Padding="0"
                                                    Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"/>
                                        </StackPanel>

                                        <Grid Grid.Row="2" Margin="0,5,0,0">
                                            <TextBlock FontSize="12"
                                                       Foreground="#4CAF50"
                                                       FontWeight="Bold"
                                                       HorizontalAlignment="Left">
                                                <Run Text="{Binding TotalPrice, Mode=OneWay, StringFormat='{}{0:N2} ₴'}"/>
                                            </TextBlock>
                                            <Button Content="Видалити"
                                                    Style="{StaticResource DangerButton}"
                                                    FontSize="11"
                                                    Padding="8,4"
                                                    HorizontalAlignment="Right"
                                                    Command="{Binding DataContext.RemoveFromCartCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"/>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Разом -->
                <Border Grid.Row="2"
                        Background="#E3F2FD"
                        BorderBrush="#2196F3"
                        BorderThickness="0,2,0,0"
                        Padding="10"
                        Margin="0,15,0,0">
                    <TextBlock FontSize="16"
                               FontWeight="Bold"
                               Foreground="#1976D2">
                        <Run Text="Разом: "/>
                        <Run Text="{Binding TotalAmount, Mode=OneWay, StringFormat='{}{0:N2} ₴'}"/>
                    </TextBlock>
                </Border>

                <!-- Вибір типу отримання -->
                <StackPanel Grid.Row="3" Margin="0,15,0,0">
                    <TextBlock Text="Спосіб отримання:"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>
                    <RadioButton Content="Самовивіз"
                                 IsChecked="{Binding IsPickup}"
                                 FontSize="12"
                                 Margin="0,0,0,5"/>
                    <RadioButton Content="Доставка"
                                 IsChecked="{Binding IsDelivery}"
                                 FontSize="12"/>
                </StackPanel>

                <!-- Кнопка оформлення -->
                <Button Grid.Row="4"
                        Content="Оформити замовлення"
                        Style="{StaticResource SuccessButton}"
                        FontSize="16"
                        Padding="0,12"
                        Margin="0,15,0,0"
                        Command="{Binding CheckoutCommand}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
