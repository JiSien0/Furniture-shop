<Window x:Class="FurnitureShop.ProductDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Детальний перегляд товару" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        ResizeMode="NoResize"
        Icon="bench.ico">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Зображення з галереєю -->
        <Border Grid.Column="0"
                Background="White"
                Margin="20">
            <Border.Effect>
                <DropShadowEffect Color="#CCCCCC" Opacity="0.3" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Головне зображення -->
                <Image Grid.Row="0"
                       x:Name="ProductImage"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="20"/>

                <!-- Навігація та мініатюри -->
                <Grid Grid.Row="1" Margin="20,0,20,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Кнопка "Назад" -->
                    <Button Grid.Column="0"
                            x:Name="PrevButton"
                            Content="◀"
                            FontSize="20"
                            Width="40"
                            Height="40"
                            Margin="0,0,10,0"
                            Click="PrevImage_Click"
                            Background="#2196F3"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"/>

                    <!-- Мініатюри -->
                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                x:Name="ThumbnailsPanel">
                    </StackPanel>

                    <!-- Кнопка "Вперед" -->
                    <Button Grid.Column="2"
                            x:Name="NextButton"
                            Content="▶"
                            FontSize="20"
                            Width="40"
                            Height="40"
                            Margin="10,0,0,0"
                            Click="NextImage_Click"
                            Background="#2196F3"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Інформація -->
        <Border Grid.Column="1"
                Background="White"
                Margin="20,20,20,20">
            <Border.Effect>
                <DropShadowEffect Color="#CCCCCC" Opacity="0.3" BlurRadius="10" ShadowDepth="2"/>
            </Border.Effect>

            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Назва -->
                <TextBlock Grid.Row="0"
                           x:Name="ProductName"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#333333"
                           TextWrapping="Wrap"
                           Margin="0,0,0,15"/>

                <!-- Ціна -->
                <Border Grid.Row="1"
                        Background="#E8F5E9"
                        CornerRadius="8"
                        Padding="15"
                        Margin="0,0,0,20">
                    <TextBlock x:Name="ProductPrice"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="#4CAF50"
                               HorizontalAlignment="Center"/>
                </Border>

                <!-- Опис -->
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Опис товару:"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="#666666"
                                   Margin="0,0,0,10"/>
                        <TextBlock x:Name="ProductDescription"
                                   FontSize="13"
                                   Foreground="#666666"
                                   TextWrapping="Wrap"
                                   LineHeight="20"/>
                    </StackPanel>
                </ScrollViewer>

                <!-- Координати складу -->
                <Border Grid.Row="3"
                        Background="#E3F2FD"
                        BorderBrush="#2196F3"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="15"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="📍 Координати на складі:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#1976D2"
                                   Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Ряд: "
                                       FontSize="13"
                                       Foreground="#1976D2"/>
                            <TextBlock x:Name="ProductRow"
                                       FontSize="13"
                                       FontWeight="Bold"
                                       Foreground="#1976D2"/>
                            <TextBlock Text=" | Полиця: "
                                       FontSize="13"
                                       Foreground="#1976D2"
                                       Margin="10,0,0,0"/>
                            <TextBlock x:Name="ProductShelf"
                                       FontSize="13"
                                       FontWeight="Bold"
                                       Foreground="#1976D2"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Кнопки -->
                <StackPanel Grid.Row="4">
                    <Button x:Name="AddToCartButton"
                            Content="Додати в кошик"
                            Style="{StaticResource SuccessButton}"
                            FontSize="16"
                            Padding="0,12"
                            Margin="0,0,0,10"
                            Click="AddToCart_Click"/>
                    <Button Content="Закрити"
                            FontSize="14"
                            Padding="0,10"
                            Background="#9E9E9E"
                            Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
